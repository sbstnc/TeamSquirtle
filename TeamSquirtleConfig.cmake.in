@PACKAGE_INIT@

set(TEAMSQUIRTLE_WITH_KDECORATION @WITH_DECORATIONS@)
if(${TEAMSQUIRTLE_WITH_KDECORATION})
    set(TEAMSQUIRTLE_KDECORATION_PLUGIN_ID "org.kde.teamsquirtle")
else()
    set(TEAMSQUIRTLE_KDECORATION_PLUGIN_ID "")
endif()

set(TEAMSQUIRTLE_STYLE_NAME "teamsquirtle")
